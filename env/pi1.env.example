# =============================================================================
# Pi1 (Primary Node) - Node-Specific Configuration
# =============================================================================
#
# This file contains ONLY the values that differ between Pi1 and Pi2.
# Use this with .env.production.example for your complete configuration.
#
# Setup Instructions:
#   1. On Pi1, copy .env.production.example to .env
#   2. Update .env with these Pi1-specific values
#   3. Set all REQUIRED passwords in .env
#   4. Run: make validate-env
#   5. Deploy: make up-core
#
# =============================================================================

# Node identification
NODE_NAME=pi1-dns

# Network configuration for Pi1
HOST_IP=192.168.8.250

# Keepalived configuration for Pi1 (MASTER role)
NODE_ROLE=MASTER
KEEPALIVED_PRIORITY=200

# Peer configuration (points to Pi2)
PEER_IP=192.168.8.251
PEER_HOSTNAME=pi2-dns

# =============================================================================
# Notes:
# - Pi1 is configured as MASTER with higher priority (200)
# - If Pi1 is healthy, it will hold the VIP
# - If Pi1 fails, Pi2 automatically takes over the VIP
# - All other settings should match Pi2 (same passwords, VIP, etc.)
# =============================================================================
