# =============================================================================
# Pi2 (Secondary Node) - Node-Specific Configuration
# =============================================================================
#
# This file contains ONLY the values that differ between Pi1 and Pi2.
# Use this with .env.production.example for your complete configuration.
#
# Setup Instructions:
#   1. On Pi2, copy .env.production.example to .env
#   2. Update .env with these Pi2-specific values
#   3. Set all REQUIRED passwords in .env (MUST match Pi1 passwords)
#   4. Run: make validate-env
#   5. Deploy: make up-core
#
# =============================================================================

# Node identification
NODE_NAME=pi2-dns

# Network configuration for Pi2
HOST_IP=192.168.8.251

# Keepalived configuration for Pi2 (BACKUP role)
NODE_ROLE=BACKUP
KEEPALIVED_PRIORITY=150

# Peer configuration (points to Pi1)
PEER_IP=192.168.8.250
PEER_HOSTNAME=pi1-dns

# =============================================================================
# Notes:
# - Pi2 is configured as BACKUP with lower priority (150)
# - Pi2 will take over VIP only if Pi1 fails or is down
# - Pi2 monitors Pi1's health and automatically fails back when Pi1 recovers
# - All other settings MUST match Pi1 (same passwords, VIP, VRRP config)
# =============================================================================
