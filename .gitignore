__pycache__/
*.pyc
.env

# BUT: Track .env symlinks in stack directories (they point to ../../.env)
!stacks/dns/.env
!stacks/observability/.env
!stacks/ai-watchdog/.env

# VPN Edition sensitive configuration files
.env.vpn
**/.env.vpn
*.env.vpn

# WireGuard configuration (contains private keys and peer configs)
**/wireguard/*.conf
**/wireguard/*.key
**/wireguard/*.png
config/wireguard/*
!config/wireguard/README.md

# Docker compose override files (user-specific customizations)
docker-compose.override.yml

# Signal CLI configuration (contains sensitive keys and registration data)
stacks/observability/signal-cli-config/*
!stacks/observability/signal-cli-config/README.md
*.log
*-report.md

# Backups directory (created by backup-config.sh)
backups/
!backups/.gitkeep

# Runtime state directory (created by ops/orion-dns-health.sh)
run/

# TLS certificates for DoH/DoT gateway (security sensitive)
stacks/dns/blocky/certs/*.key
stacks/dns/blocky/certs/*.crt
stacks/dns/blocky/certs/*.pem
!stacks/dns/blocky/certs/.gitkeep

# Test and temporary files
.env.test
*.tmp
*.swp
*~

# Pi-hole data directories (created at runtime)
pihole/
keepalived/config/

# Promtail runtime data
promtail/positions.yaml
